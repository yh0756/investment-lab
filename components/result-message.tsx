import { AlertTriangle, CheckCircle2, Info } from "lucide-react";
export function ResultMessage({ conclusion, reason, warning }: { conclusion: string; reason: string; warning?: string }) { return <div className="rounded-2xl border border-blue-100 bg-blue-50 p-5"><div className="flex gap-3"><CheckCircle2 className="mt-0.5 h-5 w-5 shrink-0 text-brand"/><div><p className="font-black text-navy">{conclusion}</p><p className="mt-2 text-sm leading-6 text-slate-700">{reason}</p></div></div>{warning&&<div className="mt-4 flex gap-3 rounded-xl border border-amber-200 bg-amber-50 p-3"><AlertTriangle className="mt-0.5 h-4 w-4 shrink-0 text-caution"/><p className="text-sm leading-6 text-slate-700">{warning}</p></div>}<div className="mt-3 flex items-center gap-2 text-xs text-slate-500"><Info className="h-3.5 w-3.5"/>입력값이 바뀌면 설명도 자동으로 갱신됩니다.</div></div>; }
